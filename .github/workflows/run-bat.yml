name: Run .bat Script

on:
  workflow_dispatch:  # 手动触发

jobs:
  run-bat:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3  # 拉取仓库代码

    - name: Run .bat script
      run: |
        cd .minecraft
        run.bat
